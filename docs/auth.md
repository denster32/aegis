# Authentication

1. `AEGIS_ACCESS_TOKEN` / `XAI_ACCESS_TOKEN`  
2. `~/.local/share/aegis/auth.json`  
3. `~/.grok/auth.json` (Grok CLI OIDC)  
4. `XAI_API_KEY`

```bash
aegis auth status
aegis login
aegis login --import-grok
```
